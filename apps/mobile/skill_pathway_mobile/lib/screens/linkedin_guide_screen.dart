import 'package:flutter/material.dart';

class LinkedInGuideScreen extends StatelessWidget {
  const LinkedInGuideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("LinkedIn Guide")),
      body: const Center(child: Text("LinkedIn Guide Placeholder")),
    );
  }
}
